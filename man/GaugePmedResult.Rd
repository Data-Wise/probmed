% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gauge-pmed.R
\name{GaugePmedResult}
\alias{GaugePmedResult}
\title{Gauge-Calibrated Proportion Mediated Result}
\usage{
GaugePmedResult(
  p_med = integer(0),
  p_med_ci = integer(0),
  p_med_fieller = numeric(0),
  fieller_type = NA_character_,
  W = integer(0),
  W_ci = integer(0),
  W_se = integer(0),
  W_p = integer(0),
  W_ci_wald = numeric(0),
  weak_id = NA,
  weak_id_ratio = NA_real_,
  oe_snr = NA_real_,
  oe_regular = NA,
  weak_id_ratio_threshold = 3,
  oe_snr_threshold = 2,
  OE = integer(0),
  IDE = integer(0),
  IIE = integer(0),
  R = integer(0),
  theta = integer(0),
  method = character(0),
  n = integer(0),
  ci_level = integer(0),
  se_method = "analytic",
  reps = 1L,
  call = NULL
)
}
\arguments{
\item{p_med}{Numeric: interventional proportion mediated, \code{IIE / OE}.}

\item{p_med_ci}{Numeric length-2: confidence interval for \code{p_med}.}

\item{p_med_fieller}{Numeric: Fieller confidence set for the ratio \code{p_med}
(may be unbounded; empty if not requested).}

\item{fieller_type}{Character: type of Fieller set returned (e.g. bounded,
unbounded, or empty); \code{NA} if not computed.}

\item{W}{Numeric: gauge residual \code{R / OE}.}

\item{W_ci}{Numeric length-2: confidence interval for \code{W}.}

\item{W_se}{Numeric: standard error of \code{W}.}

\item{W_p}{Numeric: two-sided p-value for \code{H0: W = 0}.}

\item{W_ci_wald}{Numeric length-2: symmetric Wald interval for \code{W}, retained
as the reference for the weak-identification diagnostic even under
\code{se_method = "bootstrap"} (where \code{W_ci} holds the percentile interval).}

\item{weak_id}{Logical: weak-identification flag -- \code{TRUE} when the percentile
CI for \code{W} is at least 3x wider than the symmetric Wald interval, the regime
where the ratio \code{W = R/OE} is least trustworthy (Zhan 2026). \code{NA} unless
\code{se_method = "bootstrap"} (both intervals are required to compare), and also
\code{NA} (not \code{FALSE}) in the degenerate case where the Wald interval has
zero width. The 3x threshold is calibrated above the ~2x width gap that the
anti-conservative Wald interval shows even under strong identification.

\strong{An absent flag does not buy nominal coverage.} Independently of the
flag, the symmetric Wald interval for \code{W} is anti-conservative
\emph{everywhere}: \strong{~0.88} overall across the 16,000-rep coverage grid
(\code{inst/sim/results/gauge_boot_coverage_nsim2000.csv}), never reaching the
nominal 0.95 in any cell. So \code{weak_id = FALSE} cannot mean "this interval
is trustworthy" -- at best it means "not the worst tail of an already
sub-nominal default". If you need coverage you can rely on, use
\code{se_method = "bootstrap"} -- but know what you are buying: the percentile
interval covered \strong{1.00} in all 8 cells of that grid, which is not
"mildly" conservative, it is uncalibrated in the safe direction. You get a
guarantee at the cost of an interval wide enough to be uninformative near the
null. Neither arm is nominal; pick the error you can live with. The flag's own
operating characteristics against coverage are \strong{not yet quantified} --
see the validation grid scripted at
\code{inst/sim/hopper/run_weakid_validation.R}.

\strong{It trades sensitivity for specificity -- provisionally.} A pilot
(\code{inst/sim/pilot/weak_id_pilot.R}; 120 draws, n = 1500, \code{B = 200},
continuous \code{Y}) gives, conditioning per draw:
\itemize{
\item false alarms, \code{flag | oe_snr >= 3}: \strong{0/58}, exact 95\% CI
\code{[0.00, 0.06]} -- so the false-alarm rate is bounded near 6\%, not shown to
be zero.
\item detection, \code{flag | oe_snr <= 1.2}: \strong{12/28 = 0.43}, exact 95\%
CI \code{[0.24, 0.63]} -- it misses roughly half the cases it targets, though the
interval is wide.
}
\strong{These are pilot indications, not settled operating characteristics} (a
further 34 draws fall between the two regimes and are not summarised by either
figure). The \emph{mechanism} is firmer than the magnitudes:
\code{weak_id_ratio} is highly variable draw-to-draw in the weak regime (SD
approaching its mean), which necessarily costs detection. Pending the grid, the
safe reading is that \code{weak_id = FALSE} is \emph{uninformative} rather than
reassuring. Consult \code{oe_snr} and the Fieller set alongside it;
\code{oe_regular} is observed to catch draws that \code{weak_id} misses, so the
two gates look complementary rather than redundant.}

\item{weak_id_ratio}{Numeric: ratio of the percentile \code{W}-interval width to the
Wald \code{W}-interval width; \code{NA} if not computed.}

\item{oe_snr}{Numeric: signal-to-noise of the denominator, \code{|OE| / se(OE)}.
Small values indicate \code{OE} near 0, where \code{W = R/OE} is non-regular.}

\item{oe_regular}{Logical: \code{TRUE} when \code{oe_snr >= 2}; \code{FALSE} flags a
near-singular \code{OE} for which the bootstrap CI for \code{W} may be invalid
(Lin et al. 2026). Computed unconditionally from the analytic se(OE) regardless
of \code{se_method} (only the accompanying \code{warning()} is gated on
\code{se_method = "bootstrap"}, since that's the only mode where a bootstrap CI
for \code{W} is actually being reported). \code{NA} only in the degenerate case
\code{se(OE) == 0}.}

\item{weak_id_ratio_threshold, oe_snr_threshold}{Numeric: the threshold values
actually used for the \code{weak_id}/\code{oe_regular} gates on this result (see
the identically-named arguments of \code{\link{ward_residual}}). Stored so
\code{print()} can report the threshold that was actually applied, not a
hardcoded default.}

\item{OE, IDE, IIE, R}{Numeric: overall, interventional direct, interventional
indirect effects and the remainder.}

\item{theta}{Numeric length-4: corner means \verb{theta(a, a')}.}

\item{method}{Character: estimation method.}

\item{n}{Integer: sample size.}

\item{ci_level}{Numeric: confidence level.}

\item{se_method}{Character: \code{"analytic"} (influence-function se) or
\code{"bootstrap"} (nonparametric, valid but mildly conservative).}

\item{reps}{Integer: number of repeated cross-fitting fold draws averaged for
the point estimate.}

\item{call}{Call: original call.}
}
\description{
S7 class for the gauge-calibrated proportion mediated. Reports the
interventional proportion mediated \code{P_med = IIE / OE} alongside the
\strong{gauge residual} \code{W = R / OE}, where \code{R = OE - IDE - IIE} is the
non-decomposability (treatment-by-mediator interaction) term. A \code{W}
significantly different from zero signals that the additive split of the
overall effect fails and the single-number \code{P_med} is unreliable.
}
