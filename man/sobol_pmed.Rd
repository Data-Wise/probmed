% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sobol-pmed.R
\name{sobol_pmed}
\alias{sobol_pmed}
\title{Sobol / Variance-Scale Proportion Mediated}
\usage{
sobol_pmed(
  object,
  pd = 0.5,
  pm = 0.5,
  covars = "C",
  K = 5L,
  seed = 1L,
  ci_level = 0.95,
  warn_boundary = TRUE,
  boundary_test = c("split", "plugin", "none"),
  procedure = c("B", "A"),
  reps = 1L,
  se_method = c("analytic", "bootstrap"),
  B = 200L,
  ...
)
}
\arguments{
\item{object}{A \code{data.frame} with columns \code{A} (binary treatment), \code{M}
(mediator), \code{Y} (continuous outcome), and the covariates named in \code{covars}.}

\item{pd, pm}{Numeric: tilt probabilities for the direct and mediated pathways
(Bernoulli design points); defaults \code{0.5}.}

\item{covars}{Character vector of covariate column names. Default \code{"C"}.}

\item{K}{Integer: number of cross-fitting folds (default \code{5}).}

\item{seed}{Integer: RNG seed for fold assignment and the boundary split
(default \code{1}).}

\item{ci_level}{Numeric: confidence level (default \code{0.95}).}

\item{warn_boundary}{Logical: warn when the boundary test does not reject and
the reported interval falls back to the Procedure-A upper bound
(default \code{TRUE}).}

\item{boundary_test}{Character: boundary test for \code{H0: V_med = 0}. \code{"split"}
(default) is the Williamson sample-split test on \code{Delta_m}; \code{"plugin"} is the
(over-rejecting) plug-in Wald test, diagnostic only.}

\item{procedure}{Character: interval procedure for \code{ci}. \code{"B"} (default) is the
sample-split CI (image of the \code{Delta_m} CI); \code{"A"} is the legacy gated interval
(non-uniform near the boundary). See \link{SobolPmedResult}.}

\item{reps}{Integer: repeated cross-fitting draws averaged for the point estimate
(default \code{1}). Use \code{reps > 1} near the boundary for a reproducible, lower-variance
estimate (it removes the ~80\% fold-split variance).}

\item{se_method}{Character: \code{"analytic"} (default) or \code{"bootstrap"}. The bootstrap
gives a valid (conservative) se near the non-regular boundary, where the analytic
se is anti-conservative; off the boundary the analytic se is calibrated.}

\item{B}{Integer: bootstrap resamples when \code{se_method = "bootstrap"} (default \code{200}).}

\item{...}{Unused.}
}
\value{
A \link{SobolPmedResult} object.
}
\description{
Estimate the \strong{Sobol proportion mediated} \code{P_med^{sigma2} = V_med / V_T} -- the
share of the total interventional outcome variance carried by the mediated
pathway, i.e. the first-order Sobol (functional-ANOVA) sensitivity index of the
outcome with respect to the mediator. The estimator is cross-fitted and built
on the same triply-robust corner pseudo-outcomes as \code{\link[=ward_residual]{ward_residual()}} and
\code{\link[=incr_pmed]{incr_pmed()}}; the standard error uses the delta method through the quadratic
variance components and the ratio identity. See \link{SobolPmedResult} for the
variance decomposition and the boundary remedy.
}
\details{
At the null \code{V_med = 0} (equivalently \code{Delta_m = 0}) the squared-variance
influence function degenerates and the symmetric Wald interval is non-regular.
A Williamson et al. (2021) sample-split test on the regular contrast \code{Delta_m}
decides the boundary. The default reported interval is \strong{Procedure B}
(\code{procedure = "B"}): the image of the regular \code{Delta_m} Wald CI under
\verb{delta -> (c_m/V_T) delta^2} (continuous mapping, no gate). Its validity inherits
that of the input \code{Delta_m} CI -- so near the boundary use \code{reps > 1} and
\code{se_method = "bootstrap"} for a valid interval (the analytic \code{Delta_m} se is
anti-conservative there; see the boundary coverage section). \strong{Procedure A}
(\code{procedure = "A"}) is the legacy gated rule (Wald on rejection, one-sided
\verb{[0, Pmed_upper]} otherwise), pointwise- but \strong{not} uniformly-valid across the
near-null transition (Leeb-Potscher pre-test under-coverage). The boundary
machinery recurses through a plain internal fitter (not this generic), so the
point estimate, fold draw, and split are deterministic in \code{seed}. See the
boundary coverage section in \link{SobolPmedResult}.
}
\examples{
set.seed(1)
n <- 1500; C <- rnorm(n)
A <- rbinom(n, 1, plogis(-0.2 + 0.8 * C))
M <- 0.6 * A + 0.4 * C + rnorm(n)
# strong mediation (off-boundary): Wald interval reported
Y <- 0.5 * A + 0.7 * M + 0.3 * C + rnorm(n)
sobol_pmed(data.frame(A, M, Y, C))

}
\references{
Williamson, B. D., Gilbert, P. B., Carone, M., & Simon, N. (2021).
Nonparametric variable importance assessment using machine learning
techniques. \emph{Biometrics}, 77(1), 9--22.
}
