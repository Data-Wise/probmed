% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-parallel.R
\name{.pmed_parallel_closed}
\alias{.pmed_parallel_closed}
\title{Closed-form joint P_med for parallel mediators (Gaussian).}
\usage{
.pmed_parallel_closed(a_vec, b_vec, sigma_m_vec, sigma_y, delta)
}
\arguments{
\item{a_vec, b_vec}{Length-k path vectors (X -> Mj, Mj -> Y).}

\item{sigma_m_vec}{Length-k mediator residual SDs.}

\item{sigma_y}{Outcome residual SD (scalar).}

\item{delta}{Treatment contrast \code{x_value - x_ref}.}
}
\value{
Scalar joint P_med in \verb{[0, 1]}.
}
\description{
For k parallel mediators with treatment fixed at the treated level for both
potential outcomes, \code{Y_t - Y_c ~ N(delta * sum(a*b), 2*sum(b^2*Vm) + 2*Vy)},
so the joint P_med is \code{Phi(delta*sum(a*b) / sqrt(2*sum(b^2*Vm) + 2*Vy))}.
Recovers the single-mediator formula at k = 1.
}
\keyword{internal}
