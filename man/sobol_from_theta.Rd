% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sobol-pmed.R
\name{sobol_from_theta}
\alias{sobol_from_theta}
\title{Map corner means to the Sobol variance decomposition}
\usage{
sobol_from_theta(theta, pd = 0.5, pm = 0.5)
}
\arguments{
\item{theta}{Numeric length-4: corner means named \code{"11"}, \code{"10"}, \code{"01"},
\code{"00"} (i.e. \verb{theta(a, a')} for \verb{(a, a')} in the four corners).}

\item{pd, pm}{Numeric: tilt probabilities for the direct and mediated pathways
(Bernoulli design points); defaults \code{0.5}.}
}
\value{
A named numeric vector with elements \code{Vd}, \code{Vm}, \code{Vdm}, \code{VT},
\code{Pmed_sobol} (= \code{Vm / VT}), \code{S1_med} (= \code{Vm / VT}),
\code{ST_med} (= \code{(Vm + Vdm) / VT}), and \code{Rint} (the interaction remainder).
}
\description{
Closed-form map from the four corner means \verb{theta(a, a')} to the Sobol /
functional-ANOVA variance components and the variance-scale proportion
mediated. Used internally by \code{\link[=sobol_pmed]{sobol_pmed()}} and exported as a helper for
computing the analytic (population) truth from known corner means.
}
\examples{
th <- c("11" = 1.32, "10" = 0.5, "01" = 0.42, "00" = 0)
sobol_from_theta(th)

}
