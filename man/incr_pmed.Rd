% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental-pmed.R
\name{incr_pmed}
\alias{incr_pmed}
\title{Incremental Mediated Elasticity Curve}
\usage{
incr_pmed(
  object,
  deltas = c(1/3, 1/2, 1, 2, 3),
  covars = "C",
  K = 5L,
  ci_level = 0.95,
  seed = 1L,
  ...
)
}
\arguments{
\item{object}{A data frame with columns \code{A} (binary treatment), \code{M}
(mediator), \code{Y} (outcome), and the covariates named in \code{covars}.}

\item{deltas}{Numeric: tilt factors at which to evaluate the curve.}

\item{covars}{Character: covariate column names (default \code{"C"}).}

\item{K}{Integer: number of cross-fitting folds (default \code{5}).}

\item{ci_level}{Numeric: confidence level (default \code{0.95}).}

\item{seed}{Integer: random seed for fold assignment (default \code{1}).}

\item{...}{Unused.}
}
\value{
An \link{IncrPmedResult} object whose \verb{@curve} slot holds one row per
\code{delta} with the direct, mediated, and total elasticities, the proportion
mediated \code{Pmed}, and its standard error and Wald interval.
}
\description{
Estimate the incremental mediated elasticity \code{P_med^delta(delta)} -- the
derivative-scale proportion mediated as a function of the treatment-tilt factor
\code{delta}. The estimator is cross-fitted and built on the same triply-robust
corner pseudo-outcomes as \code{\link[=ward_residual]{ward_residual()}}; standard errors use the
ratio-identity influence function for \code{med / tot} at each \code{delta}.
}
\details{
At each \code{delta} the tilted assignment probability is
\code{q = delta * g / (delta * g + 1 - g)} with derivative
\verb{q' = g (1 - g) / (delta * g + 1 - g)^2}, where \code{g = P(A = 1 | C)}. The
direct and mediated elasticities are weighted contrasts of the four corner
pseudo-outcomes by \code{q} and \verb{q'}; by Theorem 1 (multivariate chain rule) they
sum to the total elasticity exactly, so \code{P_med^delta = med / (dir + med)}.

\strong{Standard-error caveat.} The reported SE uses the ratio-identity influence
function for the corner-mean pseudo-outcomes but treats the estimated
propensity \code{g(C)} entering the tilt weights \code{q}, \verb{q'} as fixed. The interval is
valid when \code{g} is correctly specified (as here, a logistic working model that
is also the data-generating mechanism in the package tests) and is mildly
conservative otherwise. The full efficient influence function carries an
additional g-score term; adding it is tracked for a future release.
}
\examples{
set.seed(1)
n <- 1500
C <- rnorm(n)
A <- rbinom(n, 1, plogis(-0.2 + 0.8 * C))
M <- 0.6 * A + 0.4 * C + rnorm(n)
Y <- 0.5 * A + 0.7 * M + 0.3 * C + rnorm(n)
d <- data.frame(A, M, Y, C)
incr_pmed(d, deltas = c(0.5, 1, 2))

}
