% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserstein-pmed.R
\name{wasserstein_pmed}
\alias{wasserstein_pmed}
\title{Wasserstein / Transport-Scale Proportion Mediated}
\usage{
wasserstein_pmed(
  object,
  covars = "C",
  n_mc = 20000L,
  binY = FALSE,
  method = c("eif", "bootstrap", "dr"),
  n_boot = 200L,
  K = 5L,
  G = 300L,
  ci_level = 0.95,
  seed = 1L,
  ...
)
}
\arguments{
\item{object}{A \code{data.frame} with columns \code{A} (binary 0/1), \code{M} (mediator),
\code{Y} (outcome), and the covariates named in \code{covars}.}

\item{covars}{Character vector of covariate column names. Default \code{"C"}.}

\item{n_mc}{Integer: Monte-Carlo draws for g-comp corner laws (default 20000).
Ignored when \code{method = "dr"}.}

\item{binY}{Logical: if \code{TRUE}, treats \code{Y} as binary (uses logistic g-comp).}

\item{method}{Character: \code{"eif"} (default), \code{"bootstrap"}, or \code{"dr"}.}

\item{n_boot}{Integer: bootstrap replications (default 200). Used by
\code{"bootstrap"} and \code{"dr"} methods, and automatically when near the boundary.}

\item{K}{Integer: cross-fitting folds for \code{method = "dr"} (default 5).}

\item{G}{Integer: y-grid points for \code{method = "dr"} (default 300).}

\item{ci_level}{Numeric: confidence level (default 0.95).}

\item{seed}{Integer: RNG seed (default 1).}

\item{...}{Unused.}
}
\value{
A \link{WassersteinPmedResult} object.
}
\description{
Estimate \code{P_med^W = NIE^W / (NIE^W + NDE^W)}, the transport-scale proportion
mediated, where \code{NIE^W = W_2(nu_11, nu_10)} and \code{NDE^W = W_2(nu_10, nu_00)} are
the 2-Wasserstein distances between the corner laws \verb{nu(a, a')}.

Three estimation methods are available:
\itemize{
\item \code{"eif"} (default): parametric g-computation corner samples + EIF-based SE.
Valid when the g-comp working models are correct. Fast; d = 1 only.
\item \code{"bootstrap"}: parametric g-computation + bootstrap CI. Slower but does not
require the EIF to be non-degenerate; used automatically near the NIE^W = 0
boundary.
\item \code{"dr"}: cross-fit DR corner-CDF estimator (multiply robust: consistent when
either the outcome or propensity model is wrong, mediator model correct) with
bootstrap SE. Slowest; d = 1 only.
}
}
\examples{
set.seed(1)
n <- 800; C <- rnorm(n)
A <- rbinom(n, 1, plogis(0.3 * C))
M <- 0.7 * A + 0.5 * C + rnorm(n)
Y <- 0.6 * A + 0.8 * M + 0.3 * C + rnorm(n)
wasserstein_pmed(data.frame(A, M, Y, C), covars = "C")

}
