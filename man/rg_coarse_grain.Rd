% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rg-flow.R
\name{rg_coarse_grain}
\alias{rg_coarse_grain}
\title{Coarse-grain multilevel data to aggregation scale \code{m}}
\usage{
rg_coarse_grain(
  data,
  m,
  treatment = "A",
  mediator = "M",
  outcome = "Y",
  cluster = "cluster"
)
}
\arguments{
\item{data}{Data frame with the treatment, mediator, outcome, and cluster
columns.}

\item{m}{Positive integer block size. \code{m = 1} returns the unit scale
unchanged.}

\item{treatment, mediator, outcome}{Character: column names of \code{A}, \code{M}, \code{Y}.}

\item{cluster}{Character: column name of the cluster identifier.}
}
\value{
List with \code{Z} (numeric matrix of coarse-grained units, columns
\code{A}, \code{M}, \code{Y}) and \code{cluster} (their cluster labels).
}
\description{
Applies the coarse-graining operator \code{T_m}: within each cluster, units are
randomly partitioned into blocks of size \code{m} and each block is replaced by
its mean. The cluster structure is preserved, so every scale keeps the same
clusters and two scales remain comparable cluster-by-cluster.
}
\details{
Blocks are complete by construction: within a cluster of size \code{n_s}, the
final incomplete block is discarded, so the number of coarse-grained units
is \code{sum(floor(n_s / m))}, not \code{n / m}. Clusters smaller than \code{m} drop out
entirely at that scale. The composition property
\verb{T_\{m2\}(T_\{m1\}(.)) = T_\{m1 * m2\}(.)} holds exactly on the units a partition
retains.

This function calls \code{\link[=sample.int]{sample.int()}} and is therefore random. It never sets a
seed; seed the caller (or wrap in \code{withr::with_seed()}) for reproducibility.
}
\examples{
set.seed(1)
d <- data.frame(
  A = rnorm(200), M = rnorm(200), Y = rnorm(200),
  sch = rep(1:20, each = 10)
)
cg <- rg_coarse_grain(d, m = 5, cluster = "sch")
nrow(cg$Z)  # 40 = 20 clusters * floor(10 / 5)

}
