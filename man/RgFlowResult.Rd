% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rg-flow.R
\name{RgFlowResult}
\alias{RgFlowResult}
\title{Renormalization-Group Flow of the Proportion Mediated}
\usage{
RgFlowResult(
  g = integer(0),
  g_ci = integer(0),
  g_se = integer(0),
  a_path = integer(0),
  b_path = integer(0),
  TE = integer(0),
  grad_norm = integer(0),
  phi_cluster = integer(0),
  m = integer(0),
  n_units = integer(0),
  n_clusters = integer(0),
  ci_level = integer(0),
  call = NULL
)
}
\arguments{
\item{g}{Numeric: estimated proportion mediated at this scale.}

\item{g_ci}{Numeric length-2: cluster-robust EIF Wald confidence interval.}

\item{g_se}{Numeric: cluster-robust standard error.}

\item{a_path, b_path, TE}{Numeric: the \code{a}, \code{b}, and total-effect components.}

\item{grad_norm}{Numeric: Euclidean norm of the gradient of \code{g} in the five
moments; bounds the bias induced by a moment perturbation of a given size.}

\item{phi_cluster}{Numeric: per-cluster influence contributions, in cluster
order. Retained so two scales estimated on the same clusters can be
contrasted by \code{\link[=rg_flow_contrast]{rg_flow_contrast()}}.}

\item{m}{Numeric: aggregation scale (block size); \code{1} is the unit scale.}

\item{n_units}{Integer: coarse-grained units retained at this scale.}

\item{n_clusters}{Integer: clusters contributing at this scale.}

\item{ci_level}{Numeric: confidence level.}

\item{call}{Call: original call.}
}
\description{
S7 class for the \strong{scale-indexed proportion mediated}
\code{g_l = NIE_l / TE_l} estimated on data coarse-grained to aggregation scale
\code{l} (block size \code{m}). Under the coarse-graining operator \code{T_m} -- which
replaces each within-cluster block of \code{m} units by its mean -- \code{g_l} flows
with \code{m}, and the flow is governed by the mediation beta-function.

This is the classical \strong{proportion mediated} (a ratio of effects), \emph{not}
the package's \code{P_med} (a probability in \verb{[0, 1]}); the two are different
quantities and are deliberately named apart. See \code{vignette("probmed")} for
\code{P_med}.
}
\details{
\code{g_l} is a smooth functional of five second moments of \verb{(A, M, Y)}, so the
efficient influence function is available in closed form and the plug-in
estimator is already efficient in the linear-Gaussian case (no cross-fitting
required). The sampling unit is the \strong{cluster}, not the unit: influence
contributions are aggregated to the cluster before their variance is taken.
}
