% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-core.R
\name{.pmed_core_simple}
\alias{.pmed_core_simple}
\title{Simple P_med Core Computation}
\usage{
.pmed_core_simple(
  a,
  b,
  c_prime,
  x_ref,
  x_value,
  sigma_y = 1,
  sigma_m = 1,
  n_sim = 10000,
  family_y = NULL,
  family_m = NULL,
  i_y = 0,
  i_m = 0
)
}
\description{
Computes the mediation estimand
\eqn{P_{med} = P(Y(x, M(x)) > Y(x, M(x^*))) + \tfrac12 P(Y(x, M(x)) = Y(x, M(x^*)))}:
the mediator is drawn under BOTH treatment levels independently while the
treatment is held at \code{x_value} for both outcomes, so the direct effect
\code{c_prime} cancels and the result reflects mediation only. The intercepts and
families matter only for non-Gaussian models (they cancel in the Gaussian
linear contrast); their defaults keep Gaussian behaviour unchanged.
}
\keyword{internal}
