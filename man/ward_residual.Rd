% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gauge-pmed.R
\name{ward_residual}
\alias{ward_residual}
\title{Gauge-Calibrated Proportion Mediated}
\usage{
ward_residual(
  object,
  covars = "C",
  K = 5L,
  ci_level = 0.95,
  seed = 1L,
  fieller = TRUE,
  reps = 1L,
  se_method = c("analytic", "bootstrap"),
  B = 200L,
  a0 = 0,
  a1 = 1,
  weak_id_ratio_threshold = 3,
  oe_snr_threshold = 2,
  ...
)
}
\arguments{
\item{object}{A \code{data.frame} with columns \code{A} (binary treatment), \code{M}
(mediator), \code{Y} (outcome), and the covariates named in \code{covars}.}

\item{covars}{Character vector of covariate column names. Default \code{"C"}.}

\item{K}{Integer: number of cross-fitting folds (default 5).}

\item{ci_level}{Numeric: confidence level (default 0.95).}

\item{seed}{Integer: RNG seed for fold assignment.}

\item{fieller}{Logical: also compute the Fieller confidence set for the ratio
\code{P_med = IIE/OE}, which is unbounded when the total effect \code{OE} is not
significant (default \code{TRUE}).}

\item{reps}{Integer: number of repeated cross-fitting fold draws averaged for
the point estimate (default \code{1}). \code{reps > 1} averages the corner influence
matrix over independent fold assignments, removing the fold-split component
of the variance; the analytic se then adds the residual fold Monte-Carlo
variance of the averaged point.}

\item{se_method}{Character: \code{"analytic"} (default, influence-function se,
symmetric Wald CI) or \code{"bootstrap"}. Neither arm is calibrated at
\code{reps = 1}; the coverage grid
(\code{inst/sim/results/gauge_boot_coverage_nsim2000.csv}) and its decomposition
(\code{inst/sim/results/phi_decomp_summary.csv};
\code{docs/specs/FINDINGS-2026-08-22-phi-decomposition.md}) say why. The
single-partition estimator's variance is dominated by \strong{fold-split noise}
(the cross-fit partition is redrawn on every call), and its analytic se is
unstable across datasets (CV 0.5-0.8) rather than biased -- with the true
nuisances plugged in, the same se is exact -- so the symmetric Wald CI covers
only \eqn{\approx 0.85}-\eqn{0.90}. \code{reps > 1} removes the fold-split
component from the point estimate (at \code{reps = 10} it reaches the efficiency
of an oracle with known nuisances for continuous \code{Y}); a better se for that
estimator is an open item. Under \code{"bootstrap"} the CIs for \code{W} and \code{P_med}
are \strong{percentile} intervals of a nonparametric bootstrap that resamples
rows and refits the whole cross-fit (a fresh partition each resample), so
it carries the same fold-split noise and covered ~1 in every grid cell --
conservative, not calibrated; \code{W_se}/\code{p_med} se are reported as bootstrap
dispersion summaries. The bootstrap-consistency result of Lin and Han
(2026) for cross-fit DML functionals holds the nuisances fixed and does
\strong{not} cover this refit-per-resample scheme. \code{reps > 1} and
\code{se_method = "bootstrap"} compose. Near the null (\code{OE} not bounded away
from 0) the ratio is non-regular for every arm -- use the Fieller set.}

\item{B}{Integer: number of bootstrap resamples when \code{se_method = "bootstrap"}
(default \code{200}). Cost is \code{B} (x \code{reps}) refits.}

\item{a0, a1}{Reference and comparison exposure levels (defaults \code{0}/\code{1}). \code{A}
may use any two-level coding (factor, \verb{\{1,2\}}, \verb{\{-1,1\}}); it is recoded to
the binary indicator \code{(A == a1)}. The gauge \code{W = R/OE} is invariant to
swapping \code{a0}/\code{a1} (both \code{R} and \code{OE} flip sign). An \code{A} with \strong{more than
two} levels is an error, not a silent subset: restricting to \verb{\{a0,a1\}}
would shift the covariate-averaging population and estimate a different
(sub-population) gauge. Filter to the two intended levels first.
Multi-valued / continuous exposures are future work.}

\item{weak_id_ratio_threshold}{Numeric: the \code{weak_id} flag fires when the
percentile \code{W}-CI is at least this many times wider than the Wald interval
(default \code{3}). \strong{A convention, not a calibrated optimum}: in the
48,000-rep validation grid (\code{inst/sim/hopper/run_weakid_validation.R}) no
threshold in 1.75-4 was distinguishable, and the grid spans a single simulation
design -- see the \code{weak_id} property documentation of
\code{\link{GaugePmedResult}} for what the flag does and does not detect.
Exposed so it can be tuned without editing the source.}

\item{oe_snr_threshold}{Numeric: the \code{oe_regular} guard flags a near-singular
denominator when \code{|OE|/se(OE)} falls below this (default \code{2}). Validated
in the same grid as marking \strong{uninformatively wide} (over-covering)
intervals, not under-coverage -- see the \code{oe_regular} property
documentation of \code{\link{GaugePmedResult}}.}

\item{...}{Unused.}
}
\value{
A \link{GaugePmedResult} object.
}
\description{
Estimate the interventional proportion mediated together with the gauge
residual \code{W = R / OE} that flags non-decomposability (treatment-by-mediator
interaction). Uses a cross-fitted one-step estimator built on the
triply-robust efficient influence functions of the four corner means
\verb{theta(a, a') = E[Y(a, M(a'))]}.
}
\examples{
set.seed(1)
n <- 800; C <- rnorm(n)
A <- rbinom(n, 1, plogis(-0.2 + 0.8 * C))
M <- 0.6 * A + 0.4 * C + rnorm(n)
Y <- 0.5 * A + 0.7 * M + 0.8 * A * M + 0.3 * C + rnorm(n)
ward_residual(data.frame(A, M, Y, C))

}
