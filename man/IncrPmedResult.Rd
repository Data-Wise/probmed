% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental-pmed.R
\name{IncrPmedResult}
\alias{IncrPmedResult}
\title{Incremental Mediated Elasticity Result}
\usage{
IncrPmedResult(
  curve = data.frame(),
  deltas = integer(0),
  method = character(0),
  n = integer(0),
  ci_level = integer(0),
  call = NULL
)
}
\arguments{
\item{curve}{Data frame: one row per \code{delta}, columns \code{delta}, \code{dir}, \code{med},
\code{tot}, \code{Pmed}, \code{se}, \code{lo}, \code{hi}.}

\item{deltas}{Numeric: the tilt factors at which the curve was evaluated.}

\item{method}{Character: estimation method.}

\item{n}{Integer: sample size.}

\item{ci_level}{Numeric: confidence level.}

\item{call}{Call: original call.}
}
\description{
S7 class for the \strong{incremental mediated elasticity} \code{P_med^delta(delta)}
(Paper 2 of the P_med-for-modern-estimands program). Rather than a single
number, the estimand is a \emph{curve}: the share of the marginal (derivative-scale)
total effect that flows through the mediator when the treatment-assignment odds
are tilted by a factor \code{delta}.

Writing \code{Theta(delta_1, delta_2)} for the mean outcome under a \code{delta_1}-tilt of
the direct pathway and a \code{delta_2}-tilt of the mediated pathway, the elasticities
are \verb{theta'_dir = d/d delta_1 Theta}, \verb{theta'_med = d/d delta_2 Theta}, and
\verb{theta'_tot = d/d delta Theta(delta, delta)}. The multivariate chain rule gives
the \strong{exact decomposition} \verb{theta'_tot = theta'_dir + theta'_med} (remainder
\code{R(delta) = 0} for every \code{delta}), and \verb{P_med^delta(delta) = theta'_med / theta'_tot}.

Under a linear-Gaussian model with no \code{A * M} interaction the curve is flat and
equals the classical proportion mediated; it bends with \code{delta} only when an
interaction is present.
}
