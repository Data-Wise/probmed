% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rg-flow.R
\name{g_hat_eif}
\alias{g_hat_eif}
\title{Efficient-influence-function estimator of the scale-indexed proportion mediated}
\usage{
g_hat_eif(Z, cluster, m = 1, ci_level = 0.95)
}
\arguments{
\item{Z}{Numeric matrix with columns \code{A}, \code{M}, \code{Y} (coarse-grained units, as
returned by \code{\link[=rg_coarse_grain]{rg_coarse_grain()}}).}

\item{cluster}{Cluster labels for the rows of \code{Z}. The i.i.d. sampling unit
is the cluster, not the row; passing unit-level labels would understate
every standard error.}

\item{m}{Numeric: the aggregation scale \code{Z} was produced at (recorded, not
recomputed).}

\item{ci_level}{Numeric: confidence level for the Wald interval.}
}
\value{
An \link{RgFlowResult}.
}
\description{
Estimates \code{g_l = NIE_l / TE_l} on coarse-grained data by plug-in, with a
\strong{cluster-robust} standard error from the efficient influence function.
The plug-in is already efficient in the linear-Gaussian case, so no
cross-fitting is performed.
}
\examples{
set.seed(1)
d <- data.frame(
  A = rnorm(400), M = rnorm(400), Y = rnorm(400),
  sch = rep(1:40, each = 10)
)
cg <- rg_coarse_grain(d, m = 2, cluster = "sch")
fit <- g_hat_eif(cg$Z, cg$cluster, m = 2)
fit@g

}
