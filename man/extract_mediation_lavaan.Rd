% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-extract-lavaan.R
\name{extract_mediation_lavaan}
\alias{extract_mediation_lavaan}
\title{Extract Mediation Structure from lavaan Objects}
\usage{
extract_mediation_lavaan(
  object,
  treatment,
  mediator,
  outcome = NULL,
  standardized = FALSE,
  ...
)
}
\arguments{
\item{object}{A fitted lavaan object.}

\item{treatment}{Character: name of the treatment variable.}

\item{mediator}{Character: name of the mediator variable.}

\item{outcome}{Character: name of the outcome variable. If \code{NULL},
the function attempts to detect it automatically.}

\item{standardized}{Logical: whether to use standardized estimates.
Default is \code{FALSE}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{MediationExtract} object.
}
\description{
Extract mediation structure from SEM models fitted with the \code{lavaan} package.
Supports models fitted with \code{sem()}, \code{cfa()}, or \code{lavaan()}.
}
\examples{
\dontrun{
if (requireNamespace("lavaan", quietly = TRUE)) {
    library(lavaan)

    # Define model
    model <- "
    M ~ a*X
    Y ~ b*M + cp*X
  "

    # Simulate data
    X <- rnorm(100)
    M <- 0.5 * X + rnorm(100)
    Y <- 0.4 * M + 0.2 * X + rnorm(100)
    data <- data.frame(X = X, M = M, Y = Y)

    # Fit model
    fit <- sem(model, data = data)

    # Extract
    extract <- extract_mediation(fit, treatment = "X", mediator = "M")

    # Compute P_med
    pmed(extract)
}
}
}
\keyword{internal}
