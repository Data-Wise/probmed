% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental-sensitivity.R
\name{incr_sensitivity}
\alias{incr_sensitivity}
\title{M--Y Confounding Sensitivity for the Incremental Mediated Elasticity}
\usage{
incr_sensitivity(object, threshold = 0, ...)
}
\arguments{
\item{object}{An \link{IncrPmedResult} from \code{\link[=incr_pmed]{incr_pmed()}}.}

\item{threshold}{Numeric or \code{NA}: a \code{P_med} value whose tipping bias to
report per \code{delta} (default \code{0} = "bias that makes the share vanish").}

\item{...}{Unused.}
}
\value{
A data frame: one row per \code{delta} with columns \code{delta}, \code{med},
\code{tot}, \code{Pmed}, \code{tipping}, \code{tipping_threshold}.
}
\description{
Per-\code{delta} M--Y unmeasured-confounding sensitivity for an \link{IncrPmedResult}.
For each tilt factor \code{delta}, reports the additive numerator bias that zeroes
the incremental mediated share (\code{tipping}) and, optionally, the bias that
drives \code{P_med^delta} to a user \code{threshold}. Reuses \code{\link[=pmed_sensitivity]{pmed_sensitivity()}};
the additive-offset model is exact for the Paper 2 (mean-based) share.
}
\examples{
set.seed(1)
n <- 800; C <- rnorm(n)
A <- rbinom(n, 1, plogis(-0.2 + 0.8 * C))
M <- 0.6 * A + 0.4 * C + rnorm(n)
Y <- 0.5 * A + 0.7 * M + 0.3 * C + rnorm(n)
fit <- incr_pmed(data.frame(A, M, Y, C), deltas = c(0.5, 1, 2))
incr_sensitivity(fit, threshold = 0)

}
