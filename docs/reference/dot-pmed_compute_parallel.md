# Dispatch parallel (joint) P_med by inference method.

Dispatch parallel (joint) P_med by inference method.

## Usage

``` r
.pmed_compute_parallel(
  extract,
  x_ref,
  x_value,
  method,
  n_boot,
  ci_level,
  seed,
  ...
)
```
