# Wave 1 — Incremental Sensitivity Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `incr_sensitivity()` to probmed — a thin wrapper that maps an `IncrPmedResult` to a per-δ M–Y unmeasured-confounding tipping-bias table, reusing the existing `pmed_sensitivity()` helper.

**Architecture:** The incremental estimand is a *curve*: `IncrPmedResult@curve` holds one row per δ with `med` (indirect component) and `tot` (total). For each δ-row, call `pmed_sensitivity(indirect = med, total = tot, threshold = ...)` and collect its `tipping_indirect` / `tipping_threshold` into a tidy per-δ data frame. No refitting — pure arithmetic on existing columns, exact for the Paper 2 mean-based share.

**Tech Stack:** R (>= 4.1), S7, testthat (3e), roxygen2 (pinned 7.3.3), native pipe.

## Global Constraints

- R minimum 4.1.0; native pipe `|>`.
- ALWAYS explicit `package::function()` for non-base functions.
- roxygen2 pinned to 7.3.3 (8.0.0 breaks S7 method docs in check).
- Functions snake_case; internal helpers dot-prefixed; S7 classes CamelCase.
- Work on the current feature branch (`feature/gauge-bootstrap-se`) — new code files allowed on `feature/*`, NOT on `dev`.
- No pmed-modern writes — manuscript/figure/checkbox flips are out of scope for this plan (deferred, permission-gated).

---

## File Structure

- Create: `R/incremental-sensitivity.R` — `incr_sensitivity()` and its roxygen docs.
- Create: `tests/testthat/test-incremental-sensitivity.R` — unit + integration tests.
- Modify: `NAMESPACE` (regenerated by `devtools::document()`, not hand-edited).

`incr_sensitivity()` lives in its own file (not appended to `incremental-pmed.R`) because it is a distinct, optional analysis surface with its own test file — keeping it isolated keeps both files focused.

---

### Task 1: `incr_sensitivity()` core — per-δ tipping-bias table

**Files:**
- Create: `R/incremental-sensitivity.R`
- Test: `tests/testthat/test-incremental-sensitivity.R`

**Interfaces:**
- Consumes: `IncrPmedResult` (from `R/incremental-pmed.R`) — its `@curve` data frame with columns `delta`, `dir`, `med`, `tot`, `Pmed`, `se`, `lo`, `hi`; and `probmed::pmed_sensitivity(indirect, total, threshold)` returning a `PmedSensitivity` with `@tipping_indirect`, `@tipping_threshold`.
- Produces: `incr_sensitivity(object, threshold = 0, ...)` returning a `data.frame` with columns `delta`, `med`, `tot`, `Pmed`, `tipping`, `tipping_threshold` — one row per δ. `tipping` = numerator bias that zeroes the incremental share at that δ (`= -med`); `tipping_threshold` = bias driving `Pmed` to `threshold` (`= threshold*tot - med`, `NA` if `threshold` is `NA`).

- [ ] **Step 1: Write the failing test**

```r
test_that("incr_sensitivity returns one row per delta with closed-form tipping bias", {
  curve <- data.frame(
    delta = c(0.5, 1, 2),
    dir = c(0.20, 0.25, 0.30),
    med = c(0.10, 0.15, 0.05),
    tot = c(0.30, 0.40, 0.35),
    Pmed = c(0.10, 0.15, 0.05) / c(0.30, 0.40, 0.35),
    se = c(0.02, 0.02, 0.02), lo = NA, hi = NA
  )
  res <- probmed:::.incr_sensitivity_from_curve(curve, threshold = 0)
  expect_equal(nrow(res), 3L)
  expect_equal(res$delta, curve$delta)
  expect_equal(res$tipping, -curve$med)                 # b = -med
  expect_equal(res$tipping_threshold, 0 * curve$tot - curve$med)
  expect_equal(res$Pmed, curve$med / curve$tot)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `R -q -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-incremental-sensitivity.R")'`
Expected: FAIL — `.incr_sensitivity_from_curve` not found.

- [ ] **Step 3: Write minimal implementation**

```r
#' Internal: build the per-delta sensitivity table from an IncrPmedResult curve.
#' @noRd
.incr_sensitivity_from_curve <- function(curve, threshold = 0) {
  rows <- lapply(seq_len(nrow(curve)), function(i) {
    s <- probmed::pmed_sensitivity(
      indirect = curve$med[i], total = curve$tot[i], threshold = threshold
    )
    data.frame(
      delta = curve$delta[i], med = curve$med[i], tot = curve$tot[i],
      Pmed = s@p_med, tipping = s@tipping_indirect,
      tipping_threshold = s@tipping_threshold
    )
  })
  out <- do.call(rbind, rows)
  rownames(out) <- NULL
  out
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `R -q -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-incremental-sensitivity.R")'`
Expected: PASS (1 test, 5 expectations).

- [ ] **Step 5: Commit**

```bash
git add R/incremental-sensitivity.R tests/testthat/test-incremental-sensitivity.R
git commit -m "feat(incr): per-delta sensitivity table core (.incr_sensitivity_from_curve)"
```

---

### Task 2: Public `incr_sensitivity()` + roxygen export + integration test

**Files:**
- Modify: `R/incremental-sensitivity.R` (add the exported wrapper above the internal helper)
- Modify: `tests/testthat/test-incremental-sensitivity.R` (add integration test)
- Modify: `NAMESPACE` (via `devtools::document()`)

**Interfaces:**
- Consumes: `incr_pmed(data.frame, deltas, ...)` → `IncrPmedResult` (from `R/incremental-pmed.R`); `.incr_sensitivity_from_curve()` (Task 1).
- Produces: exported `incr_sensitivity(object, threshold = 0, ...)` accepting an `IncrPmedResult`, returning the per-δ data frame from Task 1.

- [ ] **Step 1: Write the failing integration test**

```r
test_that("incr_sensitivity runs end-to-end on an incr_pmed fit", {
  set.seed(1)
  n <- 800
  C <- rnorm(n)
  A <- rbinom(n, 1, plogis(-0.2 + 0.8 * C))
  M <- 0.6 * A + 0.4 * C + rnorm(n)
  Y <- 0.5 * A + 0.7 * M + 0.3 * C + rnorm(n)
  fit <- incr_pmed(data.frame(A, M, Y, C), deltas = c(0.5, 1, 2))
  sens <- incr_sensitivity(fit, threshold = 0)
  expect_s3_class(sens, "data.frame")
  expect_equal(nrow(sens), 3L)
  expect_equal(sens$tipping, -fit@curve$med)
  expect_true(all(is.finite(sens$tipping)))
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `R -q -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-incremental-sensitivity.R")'`
Expected: FAIL — `incr_sensitivity` not found.

- [ ] **Step 3: Write the exported wrapper + roxygen**

Prepend to `R/incremental-sensitivity.R`:

```r
#' M--Y Confounding Sensitivity for the Incremental Mediated Elasticity
#'
#' @description
#' Per-`delta` M--Y unmeasured-confounding sensitivity for an [IncrPmedResult].
#' For each tilt factor `delta`, reports the additive numerator bias that zeroes
#' the incremental mediated share (`tipping`) and, optionally, the bias that
#' drives `P_med^delta` to a user `threshold`. Reuses [pmed_sensitivity()];
#' the additive-offset model is exact for the Paper 2 (mean-based) share.
#'
#' @param object An [IncrPmedResult] from [incr_pmed()].
#' @param threshold Numeric or `NA`: a `P_med` value whose tipping bias to
#'   report per `delta` (default `0` = "bias that makes the share vanish").
#' @param ... Unused.
#'
#' @return A data frame: one row per `delta` with columns `delta`, `med`,
#'   `tot`, `Pmed`, `tipping`, `tipping_threshold`.
#'
#' @examples
#' set.seed(1)
#' n <- 800; C <- rnorm(n)
#' A <- rbinom(n, 1, plogis(-0.2 + 0.8 * C))
#' M <- 0.6 * A + 0.4 * C + rnorm(n)
#' Y <- 0.5 * A + 0.7 * M + 0.3 * C + rnorm(n)
#' fit <- incr_pmed(data.frame(A, M, Y, C), deltas = c(0.5, 1, 2))
#' incr_sensitivity(fit, threshold = 0)
#'
#' @export
incr_sensitivity <- function(object, threshold = 0, ...) {
  if (!inherits(object, "probmed::IncrPmedResult") &&
      !methods::is(object, "probmed::IncrPmedResult")) {
    stop("`object` must be an IncrPmedResult (from incr_pmed()).", call. = FALSE)
  }
  .incr_sensitivity_from_curve(object@curve, threshold = threshold)
}
```

- [ ] **Step 4: Regenerate docs/NAMESPACE**

Run: `R -q -e 'devtools::document()'`
Expected: writes `man/incr_sensitivity.Rd`; adds `export(incr_sensitivity)` to `NAMESPACE`. No roxygen errors (roxygen2 7.3.3).

- [ ] **Step 5: Run the full incremental + sensitivity tests**

Run (rforge, from `/Users/dt/projects/dev-tools/rforge`): `python3 -m lib.rcmd --kind test --path /Users/dt/projects/r-packages/active/probmed`
Expected: 0 failed; the new test file passes; suite count rises from 326.

- [ ] **Step 6: Commit**

```bash
git add R/incremental-sensitivity.R tests/testthat/test-incremental-sensitivity.R NAMESPACE man/incr_sensitivity.Rd
git commit -m "feat(incr): export incr_sensitivity() — per-delta M-Y tipping bias"
```

---

## Self-Review

**Spec coverage (Wave 1 of the design doc):**
- "feed per-δ `(med, tot)` into `pmed_sensitivity()`" → Task 1.
- "tipping-bias per δ" → Task 1 (`tipping = -med`), Task 2 (end-to-end).
- "surface decision (a) thin wrapper" → Task 2 (`incr_sensitivity()`).
- "focused test: round-trip, tipping `= -med`, threshold `= thr*tot - med`, finite/positive" → Task 1 (closed-form) + Task 2 (finite on a real fit).
- pmed-modern manuscript half → explicitly **out of scope** (deferred, gated) per Global Constraints.

**Placeholder scan:** none — all steps carry real code and exact run commands.

**Type consistency:** `.incr_sensitivity_from_curve(curve, threshold)` and `incr_sensitivity(object, threshold, ...)` consistent across Tasks 1–2; column set `delta, med, tot, Pmed, tipping, tipping_threshold` identical in both tasks; `@curve`, `@tipping_indirect`, `@tipping_threshold`, `@p_med` match the verified class definitions in `R/incremental-pmed.R` and `R/sensitivity.R`.
