# Assemble a bootstrap `PmedResult` (shared by both parallel bootstraps).

Assemble a bootstrap `PmedResult` (shared by both parallel bootstraps).

## Usage

``` r
.pmed_parallel_boot_result(
  boot_estimates,
  ie_boot_estimates,
  ci_level,
  n_boot,
  method,
  x_ref,
  x_value,
  extract
)
```
